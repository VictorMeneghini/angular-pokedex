name: Unit tests
on: [push]
jobs:
  Unit-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🔎 Initializing unit tests..."
      - uses: actions/setup-node@v2
      with:
        node-version: '14'
      - run: npm install
      - run: npm run tests
